# Changelog

## 0.3.0

- 增加可追溯的尝试耗时、样本总耗时、重试等待、调度等待、最终尝试和样本有效 TPS。
- API 显式 `stream: true` 支持 Chat Completions / Responses，保留 SSE 顺序与到达时间；
  只从正文增量计算可见文本延迟，不宣称纯生成速率。CLI 当前仍不支持增量到达时间。
- 每次启动及续跑保存独立执行片段，报告整体吞吐、用量覆盖率、请求完成速率、实际
  峰值和平均并发。缺失部分用量时只显示已知部分吞吐，不相加请求 TPS。
- HTML 增加逐题性能、P5/P50/P95、故障和重试率、耗时分布与散点图；历史比较额外
  检查并发、重试、超时、流式和输出上限，展示长度与故障率变化。
- `performance analyze` 为旧记录生成独立派生分析，不覆盖原文，不补造不可恢复时间。

字段和兼容性见[性能说明](docs/performance.md)。原有无法定位样本的汇总 TPS 数组
改为 `summary.performance.observations`；题库与判分保持不变。0.2.0 的记录仍可读取和
离线分析，原有源码/配置哈希检查仍会拒绝跨版本续跑。所有新流式支持目前仅有离线
验证，本轮未授权也未执行付费实测。

## 0.2.0

验证见 [0.2.0 验证记录](docs/validation-0.2.md)。

- 持续补位的并发调度，按目标轮转，支持全局与每目标并发上限；新增
  `run --concurrency N`，dry-run 展示实际并发配置。
- 终端进度显示完成数、在途题目、各题耗时和本次耗时；`progress_interval`
  控制心跳间隔，默认 10 秒。
- Ctrl+C 停止新派发及后续错误重试，收齐在途结果后保存停止状态、报告并以
  130 退出；相同配置可续跑剩余样本。
- 预算改为逐请求预留；未知用量停止补位，续跑也不能绕过该停止条件。
- 增加仓库 AGENTS.md、Claude 入口、三个维护技能、贡献指南和统一离线检查。

兼容说明：题库与判分未改变，但旧版本运行的源码/配置哈希不同，不能直接在
新版本续跑或绕过校验作回归比较。旧报告仍可阅读；要继续旧任务需使用原版本
及原配置，新实验则开始新运行。全局或目标并发、进度配置改变也仍受完整配置
续跑检查约束。本版本尚未拆分实验参数与调度参数的兼容性签名。

## 0.1.0

首次交付：189 题、API/CLI、人工评阅、ModelTrace、历史比较及离线重判。
历史验证见 [2026-09-14 验证记录](docs/validation.md)，不是 0.2.0 的真实运行结果。
